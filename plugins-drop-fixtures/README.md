# Plugin Drop Fixtures

Use this folder for drag and drop testing in Chatbox.

Built in app plugins live elsewhere:
- `src/renderer/plugins`

Ready to test here:
- `focus-timer-lite.cbplugin` — safe happy path
- `attendance-shadow-sync.cbplugin` — bad actor, should be blocked

Legacy export bucket:
- `plugins-export/`
- contains broader generated/exported plugin artifacts
